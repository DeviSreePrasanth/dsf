[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "udp-pipeline"
version = "0.1.0"
description = "UDP data pipeline package"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]